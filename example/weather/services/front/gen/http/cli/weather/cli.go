// Code generated by goa v3.25.3, DO NOT EDIT.
//
// Weather HTTP client CLI support package
//
// Command:
// $ goa gen goa.design/clue/example/weather/services/front/design -o
// services/front

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	frontc "goa.design/clue/example/weather/services/front/gen/http/front/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"front (forecast|test-all|test-smoke)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "front forecast --p \"139.253.86.208\"" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		frontFlags = flag.NewFlagSet("front", flag.ContinueOnError)

		frontForecastFlags = flag.NewFlagSet("forecast", flag.ExitOnError)
		frontForecastPFlag = frontForecastFlags.String("p", "REQUIRED", "string is the payload type of the front service forecast method.")

		frontTestAllFlags    = flag.NewFlagSet("test-all", flag.ExitOnError)
		frontTestAllBodyFlag = frontTestAllFlags.String("body", "REQUIRED", "")

		frontTestSmokeFlags = flag.NewFlagSet("test-smoke", flag.ExitOnError)
	)
	frontFlags.Usage = frontUsage
	frontForecastFlags.Usage = frontForecastUsage
	frontTestAllFlags.Usage = frontTestAllUsage
	frontTestSmokeFlags.Usage = frontTestSmokeUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "front":
			svcf = frontFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "front":
			switch epn {
			case "forecast":
				epf = frontForecastFlags

			case "test-all":
				epf = frontTestAllFlags

			case "test-smoke":
				epf = frontTestSmokeFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "front":
			c := frontc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "forecast":
				endpoint = c.Forecast()
				data = *frontForecastPFlag
			case "test-all":
				endpoint = c.TestAll()
				data, err = frontc.BuildTestAllPayload(*frontTestAllBodyFlag)
			case "test-smoke":
				endpoint = c.TestSmoke()
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// frontUsage displays the usage of the front command and its subcommands.
func frontUsage() {
	fmt.Fprintln(os.Stderr, `Public HTTP frontend`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] front COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    forecast: Retrieve weather forecast for given IP`)
	fmt.Fprintln(os.Stderr, `    test-all: Endpoint for running ALL API Integration Tests for the Weather System, allowing for filtering on included or excluded tests`)
	fmt.Fprintln(os.Stderr, `    test-smoke: Endpoint for running API Integration Tests' Smoke Tests ONLY for the Weather System`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s front COMMAND --help\n", os.Args[0])
}
func frontForecastUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] front forecast", os.Args[0])
	fmt.Fprint(os.Stderr, " -p STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Retrieve weather forecast for given IP`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -p STRING: string is the payload type of the front service forecast method.`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "front forecast --p \"139.253.86.208\"")
}

func frontTestAllUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] front test-all", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Endpoint for running ALL API Integration Tests for the Weather System, allowing for filtering on included or excluded tests`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "front test-all --body '{\n      \"exclude\": [\n         \"Cum ducimus nostrum temporibus ex quo nihil.\",\n         \"Ex et.\",\n         \"Itaque itaque cupiditate voluptas quod.\"\n      ],\n      \"include\": [\n         \"Deleniti tenetur voluptatibus.\",\n         \"Hic eius.\",\n         \"Sint libero natus.\"\n      ]\n   }'")
}

func frontTestSmokeUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] front test-smoke", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Endpoint for running API Integration Tests' Smoke Tests ONLY for the Weather System`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "front test-smoke")
}
